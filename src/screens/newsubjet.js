import { View } from "react-native";
import { Text } from "react-native-paper";

export default NewSubject = ({navigation}) =>{
    return(
        <View>
            <Text>dd</Text>
        </View>
    )
}